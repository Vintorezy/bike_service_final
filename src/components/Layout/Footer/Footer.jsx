import styles from './footer.module.scss';

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <div className="container">© by BestBikes</div>
    </footer>
  );
};

export default Footer;
